# this file contains driver code intended to speed up development
if Rails.env == 'development'
 # Dir[File.join(Rails.root, "lib", "api", "*.rb")].each do |file|
  #   require file
 # end
  # All apis are required in development, please enter your driver code below
end
